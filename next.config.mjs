/** @type {import('next').NextConfig} */
const nextConfig = {
  // The manuscript and canon live OUTSIDE this app folder (in the Book root),
  // so the app can read/write your real files. Nothing here needs bundling.
};

export default nextConfig;
